pluginManagement {
    repositories {
        maven {
            name = "Fabric"
            url = "https://maven.fabricmc.net/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "worldmind"

include("core", "fabric-1.20.1", "sqlite-storage", "testkit", "game-context-api", "game-context-runtime")
